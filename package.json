{
  "name": "aturduitku",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check:secrets": "node scripts/check-secrets.mjs",
    "test:ledger": "node scripts/finance-ledger.test.mjs",
    "test:flows": "node scripts/financial-flow.test.mjs",
    "build": "npm run check:secrets && vite build",
    "qa:release": "npm run test:ledger && npm run test:flows && npm run build && node scripts/prelaunch-check.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.12.7",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
